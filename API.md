# ROJAN Backend — API Contract

This document has moved and been superseded by
**[`API_CONTRACT.md`](API_CONTRACT.md)**, which now covers the full API
surface (Authentication, Salon, Branch, Service Categories, Services,
Specialists, Availability, Booking) rather than just the auth vertical
slice this file originally documented.

The live, always-current machine-readable spec is served at
`/v3/api-docs` (Swagger UI at `/swagger-ui/index.html`).
